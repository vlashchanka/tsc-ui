{
  "name": "tsc-ui-dashboard",
  "version": "0.0.1",
  "description": "TSC UI Dashboard",
  "scripts": {
    "start": "snowpack dev --port=8084",
    "build": "snowpack build && cd build && npx http-server --port=8085",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.7.0",
    "http2-proxy": "^5.0.53",
    "snowpack": "^3.3.7"
  },
  "dependencies": {
    "svelte": "^3.38.2"
  }
}
